#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int sum = 0;
    float avg;

    for(int i = 0; i < 5; i++) {
        sum += arr[i];
    }

    avg = sum / 5.0;
    printf("Average of array elements: %.2f\n", avg);
    
    return 0;
}
