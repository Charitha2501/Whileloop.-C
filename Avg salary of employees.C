#include <stdio.h> 

struct Employee {
    char name[50];
    float salary;
};

int main() {
    int n, i;
    float totalSalary = 0.0, averageSalary;

    printf("Enter the number of employees: ");
    scanf("%d", &n);

    struct Employee employees[n];

return 0;
}
